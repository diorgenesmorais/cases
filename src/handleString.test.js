import { formatInDatePattern } from './handleString'

describe('Converting texts', () => {
    it('formatInDatePattern() should be a function', () => {
        expect(formatInDatePattern).toBeInstanceOf(Function)
    })
})
